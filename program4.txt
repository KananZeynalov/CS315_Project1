func main(){

    a[5] = [10, 10, 10, 10];
    b[5] = [10, 10, 10, 10];
    total[5] = [0, 0, 0, 0, 0];

    sizeA = a.length();
    sizeB = b.length();
    index = 0;

    while(index < sizeA){
        total[index] = a[index] + b[index];
        num = total[index]
        index = index + 1;
        cout << num; 
    };
    return 0;
}